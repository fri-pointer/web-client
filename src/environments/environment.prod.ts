import { AppEnv } from "./env.model";

export const environment: AppEnv = {
    production: true,
    apis: {
    
    },
    keycloak: {
        clientId: "",
        realm: "",
        url: ""
    }
};
