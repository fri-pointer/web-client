import { AppEnv } from "./env.model";

export const environment: AppEnv = {
    production: true,
    applicationId: "web-client",
    apis: {
    
    },
    keycloak: {
        clientId: "",
        realm: "",
        url: ""
    }
};
